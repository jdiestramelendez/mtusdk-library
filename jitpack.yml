jdk:
  - openjdk8

before_install:
  - echo "Setting up build environment"

install:
  - echo "Installing dependencies"

script:
  - ./gradlew clean
  - ./gradlew assemble publishToMavenLocal
  - find $HOME/.m2/repository -name "*mtusdk*" -type f